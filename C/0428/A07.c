//https://chatgpt.com/s/m_69f02405034c8191914adbabd8e2b4db

/*
* * A07.c - 使用指標運算計算陣列總和
 * ================================
 * 目的：示範 C 語言中的指標算術運算和陣列走訪
 *
 * 程式功能：
 * - 初始化一個包含 6 個整數的陣列
 * - 使用指標遍歷陣列元素
 * - 透過指標解參考計算所有元素的總和
 * - 顯示計算結果
 *
 * 主要概念：
 * - 指標初始化為陣列首元素地址
 * - 後置遞增運算子 (ptr++) 使指標移動至下一個元素
 * - 指標解參考 (*ptr) 存取指標所指的值
 * - 使用指標算術迴圈走訪陣列（替代陣列索引）
 */

#include <stdlib.h>
#include <stdio.h>    
int main(void) {
    int IOTA[6]={1,4,2,8,6,10};
    int i=0,sum=0;
    int *ptr=IOTA;



     while (i<6) {
        // 解參考指標取得目前元素的值，加入總和
        // *(ptr++) 的意義：先取出 ptr 所指的值，再將 ptr 向後移動 sizeof(int) 位元組
        // 後置遞增：ptr 的遞增發生在解參考之後
        sum=sum+*(ptr++);
        i++;
    }

    printf("sum=%d\n",sum);
    system("pause");
    return 0;
}


