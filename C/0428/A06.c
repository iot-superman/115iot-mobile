//
// https://chatgpt.com/s/m_69f020d23a2c8191a9e2e726707ff139
/*
 * ====================================
 * 檔案: A06.c
 * 功能: 指標算術演示程式
 * 說明: 展示 C 語言中的指標運算，包括指標加減
 *      和指標解參考(dereference)操作。
 * ====================================
 */

#include <stdlib.h>    
#include <stdio.h>    

int main(void) {
    // 宣告一個整數陣列，包含 5 個元素
    int AB[5]={13,15,17,19,11};
    
    // i 和 sum 的宣告（目前未使用）
    int i,sum=0;
    
    // 宣告一個指向整數的指標
    int *ptr;
    
    // 將指標指向陣列的第二個元素（陣列首位加 1）
    // AB+1 相當於 &AB[1]
    ptr=AB+1;
    
    // 列印指標指向的值：AB[1] = 15
    printf("*ptr=%d\n",*ptr);
    
    // 列印指標加 1 後指向的值：AB[2] = 17
    printf("*(ptr+1)=%d\n",*(ptr+1));
    
    // 列印指標減 1 後指向的值：AB[0] = 13
    printf("*(ptr-1)=%d\n",*(ptr-1));
    
    // 列印指標加 2 後指向的值：AB[3] = 19
    printf("*(ptr+2)=%d\n",*(ptr+2));

    // 暫停程式，等待使用者按鍵
    system("pause");
    
    return 0;
}